"""Layer 2 — signal ranking, the frozen order-of-operations, and cost/null models.

Everything here COMPOSES the frozen vendored harness through the adapter; nothing is
built inside it. The murder-board-forbidden forms (floating-k truncation, equal-dollar
sizing, construction-time beta-neutrality, post-hoc sector flag, single-draw null) are
never implemented — only the frozen REQUIRED forms.
"""

from __future__ import annotations
