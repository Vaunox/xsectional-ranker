# PROGRESS — Cross-Sectional Intraday Long-Short Ranker

*Program log. One appended entry per session; never edited retroactively. HEAD is the source of truth; gate tags are deliverable snapshots.*

*Status: **Phase 3 COMPLETE — GATE 3 green. The single blind Phase-1 smoke run (2026-07-11) returned an across-the-board KILL: the cross-sectional overnight-gap-REVERSAL ranker is decisively net-negative (DSR 0, CPCV path-Sharpes −11…−29, positive-fraction 0) on both signals (A, A-Z) × 3 windows × both cost bounds. Basis: DSR/CPCV net-negativity on the signal's own P&L (not beat-random, which was degraded by a null-construction bug — §7 of findings, a quarantined Phase-2 blocker that did NOT affect this KILL). Exploration-grade / survivorship-inflated / small-size-low-capacity (feasible book median ₹3–5M). Recorded in `docs/RESEARCH_FINDINGS.md`; the gap signal family is RETIRED → routes to candidate search. Freeze provenance: `pre-registration-frozen` @ `31cc292` (superseded ₹5M floor) → `pre-registration-v2` @ `7e6a829` (₹100k, blind).** The imported gate (CPCV/DSR/PBO/ledger/path-positivity) reached UNEDITED via the adapter + ONE new criterion (beat-random-percentile); composition per Ruling 1 (excess-over-null stream into CPCV/DSR/PBO); PF/expectancy/regime → logged diagnostics (Ruling 2); 6-arm ledger charging (Ruling 3); machinery-removal on the new benchmark; literal-N guard re-bound behaviorally (sentinel-spy) to the real DSR call site. k/N/gross-floor/null-percentile still unset (frozen blind at Step 2). Prior: **Phase 2 (Signal + Execution + Null layer) complete; GATE 2 green + fresh-checkout; committed + `gate-2` tag.** Feature primitives vendored (20 vendored files, TA-Lib 0.7.0); Signals A/A-Z (frozen gap%÷ATR%); the single ordered pipeline; ex-ante sector cap; risk-parity sizing; Execution-Aware truncation; cost corridor (composes frozen `round_trip_cost`); Corwin-Schultz/Abdi-Ranaldo (new Layer-2, hand-tested); Global Null Panel (seeded/reproducible, symmetric-execution proven). No Layer-3 gate, no smoke run. GATE 1 remains committed (`gate-1`). *Phase-1 layer (committed, historical):* data seam (`BrokerAdapter`/`Repository` over the inherited external Parquet cache, config path, no ingestion), hygiene (calendar/session, corp-action raw+adjusted, bad-tick, gap, liquidity), survivor-cache universe stub, full dual eligibility masks (~0% fire logged), static sector map, conservative circuit filter, gate-zero integrity, and the two load-bearing leakage suites (prefix-invariance + train/serve-skew) are in place. GATE 0 remains signed off + committed (`gate-0`).***

**Phase-1 stamps (on every artifact).** (1) survivorship-inflated upper bound — universe is a survivor-cache stub, real point-in-time reconstruction deferred to Phase 4; (2) split/bonus-adjusted, cash dividends unadjusted — bounded ex-div residual accepted for the survivor upper bound, dividend adjustment deferred to Phase 4; (3) dual masks architecturally complete but empirically unexercised (~0% fire on large-cap survivors is the correct signature, logged); (4) no Kite ingestion this phase — data read from the external Parquet cache read-only.

**Gate 0 semantics (do not over-read).** GATE 0 = **verified + frozen**, *not* wired into the research pipeline. The Phase-0 call site for every vendored function is the **verification harness itself** (ported tests, golden runners, the machinery-removal registry). Real research call sites arrive in Phase 2/3. The single-name vendored backtester is frozen/verified but deliberately **not** surfaced on the adapter — the cross-sectional book's two-engine reconciliation is new Phase-2 code (`src/vendored/VENDORED_FROM.md`).

---

## Gate checklist

| Gate | Deliverable | Status |
|---|---|---|
| Gate 0 | Scaffolding + **harness import verified** (golden-master reconciliation + predecessor tests green + provenance pinned) | ☑ green — operator signed off 2026-07-11 (`gate-0` tag) |
| Gate 1 | Data & universe layer (point-in-time, dual masks, sector map, circuit filter, gate-zero integrity) | ☑ green — committed + **CI green** (140 passed, 3 cache-dependent skipped) on the clean Windows runner; `gate-1` tag |
| Gate 2 | Signal + execution + null layer (A/A-Z, truncation, cost corridor, Global Null Panel) | ☑ green — operator cleared 2026-07-11; verified + fresh-checkout (182 passed, 3 cache-dependent skip); `gate-2` tag |
| Gate 3 | Phase-1 Upper-Bound Smoke Test complete (verdict exploration-grade) | ☑ green — single blind run done 2026-07-11; **across-the-board KILL** (net-negative, DSR 0 / CPCV −11…−29 / pos-frac 0, all 6 arms × both bounds); operator cleared to record; `RESEARCH_FINDINGS.md` written (exploration-grade). Null-construction bug quarantined as a Phase-2 blocker (didn't affect the KILL). |
| Gate 4 | Phase-2 Point-in-Time Crucible *(only if Gate 3 passed)* — the sole true-PASS gate | ☐ |
| Gate 5 | Synthesis (`RESEARCH_FINDINGS.md`) | ☐ |

## Frozen-at-implementation values (commit before the first run)
*Still unset. During the Phase-2 build these are parameterized from config and tested with clearly-**TOY**-stamped values; the real numbers are committed **blind at the Phase 2→3 boundary** before the first run.*
- `k` (names per leg) — unset (TOY during Phase-2 build)
- gross-exposure floor (₹ threshold) — unset (TOY during Phase-2 build)
- `N` (null draws per day) — unset (TOY during Phase-2 build); null **seed** is a determinism param (`seed: 20260711`), not a research DOF
- ~~liquidity floor (min median daily traded value)~~ — **committed in Phase 1**: ₹5 cr median daily traded value over a 20-day trailing lookback (`config/default.yaml` `hygiene.liquidity_floor`).
- null-percentile pass threshold — unset (Phase 2→3)

## Frozen-definition resolutions (an under-specified pre-registered item, pinned on the record)
- **A-Z normalization — resolved 2026-07-11.** `raw_gap / ATR(20)` was units-ambiguous; frozen as **gap% ÷ ATR%** (gap fraction ÷ ATR-20-in-return-units, ATR÷price). Rationale: *gap in units of the stock's normal daily move; price-neutral by construction* (the two unit-consistent spellings are algebraically identical for ranking — price cancels). **FORBIDDEN:** units-mixed `gap_fraction ÷ ATR_absolute_₹` (1/price-dependent, tilts toward high/low-priced names). Full entry: `MASTER_BLUEPRINT.md` Part V. A-Z requires an intraday→daily resample before `atr(daily,20)` (new Layer-2 code, own point-in-time test).
- **Truncation leg gross — resolved 2026-07-11.** Deep Dive 02's "sum the caps per leg" is under-specified once risk-parity weights enter (summing caps then redistributing by weight can hand a name > its own 1%-participation cap — an un-fillable order). Frozen as **`G_leg = min_i(cap_i / weight_i)`** (largest leg gross at which no name breaches its cap at its risk-parity weight); book gross = `min(G_long, G_short)`. Honors both the cap and the weights. Full entry: `docs/deep_dives/02`. Impl: `src/xsranker/execution/truncation.py`.
- **PF / expectancy / regime-stability — resolved 2026-07-11 (Phase-3 Ruling 2).** Deep Dive 04's kill-gate list named **profit factor, expectancy, and regime stability** as imported criteria, but they are **absent from the frozen harness** (only `fraction_positive`, CPCV `tenth_percentile`, and a feature-level `trend_regime` exist). Resolved as **logged diagnostics, not gates** — PF/expectancy are small new teeth-tested helpers over the net-return stream (`src/xsranker/gate/diagnostics.py`); "regime stability" folds into the market-day-conditioning diagnostic. This **preserves the one-new-criterion rule**: the only new *gate* criterion is beat-random-percentile. Recorded so the correction cannot later read as a silent gate weakening. Full entry: `docs/deep_dives/04` ("Resolved open questions").
- **DSR × null-percentile composition — resolved 2026-07-11 (Phase-3 Ruling 1).** Orthogonal corrections: the null-percentile strips per-day execution/survivorship luck (isolating selection skill), the DSR strips cross-arm search luck. Each arm's per-day quantity is redefined **net minus null median** (the excess-over-null stream); that excess — never the raw net — feeds CPCV/DSR/PBO. The percentile is the per-arm bar; the DSR owns multiplicity (so the percentile is not inflated for arm-count). No double-penalty / double-credit. Full entry: `docs/deep_dives/04`.
- **Gross floor RE-FROZEN BLIND v2 = ₹100,000 — 2026-07-11 (supersedes the Step-2 ₹5,000,000).** During the Step-3b pre-flight (blind, before any return), the frozen ₹5M floor was found to drop **48–71%** of days: the feasible neutral-book gross (median **₹3–5M**, fat low tail) is structurally small because the extreme-gap selection picks thin names and gross is the weakest-link `min` across the leg. Root cause: the Step-2 rationale was **ill-posed** — the cost model is **scale-invariant** (flat ~16–23 bps, no fixed component), so there is *no* cost-mechanical floor; the only small-size effect is integer-share **granularity**. ₹100,000 = the minimal floor at which every name forms a real whole-share position (mechanically derived, NOT survival-tuned, chosen with **zero sight of any return** — the sole reason a re-freeze is legitimate). Consequent day-drop ~5–10% (a consequence, logged not targeted); the null hits the same floor at the same rate (symmetric — confirmed). **SIZE CAVEAT stamped on every result:** small-size / low-capacity finding, ~10 bps integer-share tracking error materially present, follows into Phase-4. Provenance: `pre-registration-frozen` @ `31cc292` **stays in history as the superseded line** (recorded, not erased); v2 is a new timestamped blind freeze (`pre-registration-v2`), pushed **before** any run output. Full mechanical detail in `config/default.yaml` `execution.gross_floor_inr` + the feasibility distribution (feasibility analysis, this session).
- **CPCV/PBO structural params — PINNED PRE-RUN 2026-07-11 (Phase-3 Step-3a addendum).** They shape the gate, so they carry the same "fixed before the result" guarantee as the four blind values (none outcome-derived), committed to `config/default.yaml` `gate:` BEFORE the single blind run: **CPCV n_groups=6 / k_test=2** (C(6,2)=15 paths — a power/compute balance at ~2,700 obs), **PBO n_splits=16** (the vendored `DEFAULT_N_SPLITS`), **periods_per_year=252** (daily annualization). The headline four-value freeze (`pre-registration-frozen` @ `31cc292`) stays the provenance line; this addendum is a strictly earlier commit than any run output.

## Deferred to later phases (recorded so they are not forgotten)
- **Feature primitives** (`gap`, ATR-20, `cross_sectional_rank`) — vendored in **Phase 2** from pin `0c5c592` into `src/vendored/VENDORED_FROM.md` (identical freeze/adapter/golden pattern), where the signal layer gives them a real call site. (TA-Lib `0.7.0` enters the frozen stack here.)
- **Literal-N guard test** — omitted from the Phase-0 Check-1 port (no DSR business call sites). Re-add in **Phase 3** with the Layer-3 gate, scoped to `src/xsranker`, when `deflated_sharpe` acquires a real call site (re-adding in Phase 2 would be vacuous).

## Standing constraints (from `MASTER_BLUEPRINT.md` Part I)
- Kill-gate sacred; no tweak-to-pass; every arm charged to the effective-N ledger.
- Point-in-time correctness always; leakage tests in CI.
- Costs always modeled — as a corridor.
- Imported harness frozen; never edited; verified per Deep Dive 03.
- Phase 1 gates Phase 2; a Phase-1 pass is provisional only.

### Ledger-charge policy — the return-blind-screen bright line (operator-ruled 2026-07-12)
Which constructions charge the effective-N ledger is decided by **whether the step could fish for
a good result**, not by whether a return stream was persisted (a bookkeeping fact one could dodge):
- A construction **screened out on a return-blind, pre-registered criterion** — **D8** independence,
  prefix-invariance, eligibility, data-integrity (bands fixed in advance, never sees returns) — is
  **NOT charged.** Such a screen cannot select for a lucky pass, and effective-N exists precisely to
  price the probability of a false PASS.
- A construction that was **RUN and evaluated on returns is CHARGED, always, even if discarded** —
  it is a real bite at performance. (Had we run V, seen it fail, then tried V_resid, that second
  bite must be charged.)
- First application: candidate #2's **V / V-A were D8-screened out and are NOT charged**; **V_resid**
  (run on returns) IS charged — cumulative ledger = candidate #1's 6 arms + V_resid's 3.

### Two-gate feature policy (operator-ruled 2026-07-12) — vetting ≠ contribution
The stable is built with **two** gates, not one (the IPO advisor's own architecture — GMP was in the
model, measured, found not to contribute, and removed):
- **Entry gate — standalone vetting.** A feature enters the pool ONLY by showing a **real, independent
  edge alone** (beats random + independent axis). Gap A-Z: **yes** (banked). V_resid: **no** (coin
  flip → dead). This is what stops the pool becoming a **feature zoo** — you do NOT throw ~250 features
  in and let the model sort them out (that overfits ~2,200 noisy days, and the effective-N-deflated
  DSR bar — now 8.97 — correctly kills it).
- **Retention gate — marginal contribution.** Once in the model, a feature stays only if it
  **measurably improves the held-out metric**, and is cut if it does not (cf. GMP).
- **Vetting gets you into the room; contribution keeps you there.** A **standalone-KILL** (dead to
  trade alone) with a real edge is **BANKED** as a provisional feature, not deleted — killing every
  slightly-positive component destroys the ensemble's building blocks (capstone §2). A **zero-edge**
  result (V_resid) is genuinely dead: nothing to bank. **Small ≠ zero.**
- Stable membership (2026-07-12): **1 provisional member** — candidate #1 gap A-Z (banked,
  survivorship-asterisked). Candidate #2 volume-delta: fully dead (V/V-A momentum; V_resid zero edge).

---

## Session log

| Date | Phase | What | Verification | Artifacts | Verdict/Notes |
|---|---|---|---|---|---|
| 2026-07-11 | Phase 0 | Scaffolding (config/logging/CI) + vendored harness (16 files @ pin `0c5c592`) behind `HarnessAdapter` | 4 checks green: Check 1 (52 ported tests), Check 2 (6 goldens, drift 0.0), Check 3 (14 surface fns falsified 14/14), Check 4 (stack pinned Py3.11/numpy2.4.6/scipy1.17.1, seed reproducible). Full gate: **104 passed**; ruff/black/mypy-strict clean. Baseline proven at pin (predecessor `375 passed`) | `src/vendored/` + `VENDORED_FROM.md` (operator-signed) + `vendored_hashes.txt`; `tests/golden/` (6); CI + hash tripwire + freeze boundary | **Gate 0 green; operator signed off 2026-07-11; `gate-0` tag cut.** Documented omissions accepted: 1 vacuous ledger source-scan guard removed (re-add Phase 2, scoped to `src/xsranker`); 2 strategy-layer-coupled robustness tests deferred (two-engine reconciliation still covered by `test_validation_core.py`). Feature primitives deferred to Phase 1 from same pin. Phase 1 closed pending separate sign-off. |
| 2026-07-11 | Phase 2 | Signal + Execution + Null layer. Feature primitives vendored (+4 files → 20; TA-Lib 0.7.0 pinned) behind the adapter; OHLCV bridge (UTC→IST, gate-zero-class tests). Signals A/A-Z (frozen **gap% ÷ ATR%**; point-in-time ATR shift — day D uses D-1). One ordered pipeline (`eligible_pools` → rank → ⌈k/2⌉ sector cap → risk-parity sizing → Execution-Aware truncation → gross-floor day-drop) with `finalize` extracted as the SHARED path. Cost corridor (2 bounds via `dataclasses.replace` on frozen `round_trip_cost`). Corwin-Schultz/Abdi-Ranaldo spreads (new Layer-2, hand-tested). Global Null Panel (seeded, reproducible, versioned, sliceable). | **Symmetric-execution proof:** signal `build_book` and null `build_random_book` both route through the SAME `finalize` (monkeypatch spy sees both); forced-selection case → byte-identical economic book (same gross + {name,weight,notional} sets). **Seeded reproducibility:** same seed → byte-identical `NullPanel` (`p1 == p2`), different seed differs (`p1 != p3`). GATE-2 fresh-checkout (staged-tree `git archive` + fresh `uv sync`): ruff/black/mypy-strict clean (75 files), **182 passed, 3 skipped** (cache-dependent), all **9 goldens** reconcile bit-for-bit, hash tripwire + adapter-only `lab.*` boundary intact, vendored count 20. | `src/xsranker/{execution,signals,null}/`; `tests/golden/feature_{gap,atr,cross_sectional_rank}.json`; `docs/deep_dives/02` (truncation resolution); `MASTER_BLUEPRINT.md` Part V (A-Z) | **GATE 2 green; holding before commit (pending review).** k/N/gross-floor stay **TOY** (unset — no signal peeking). No Layer-3 gate, no smoke run. **Freeze catch (self-surfaced):** regenerating goldens had stamped `talib` into the meta of the six frozen Phase-0 masters (values bit-identical, but their birth env had no TA-Lib and the reconciliation test never asserts that line). Scoped `talib` in `_meta()` to the three feature goldens only and reverted the six to frozen bytes, so committed goldens == generator output AND the Phase-0 masters are byte-untouched — the Phase-2 golden diff is now exactly the three new feature files. |
| 2026-07-11 | Phase 3 | Validation & Gate layer (Layer 3) + the single blind Phase-1 smoke run. Imported gate (CPCV/DSR/PBO/ledger/path-positivity) via adapter + ONE new criterion (beat-random-percentile); composition = excess-over-null stream (Ruling 1); PF/expectancy/regime → diagnostics (Ruling 2); 6-arm ledger charge (Ruling 3). Run harness (P&L engine: entry→close hold return [no-lookahead teeth], book→net-return [short-sign/neutrality teeth], per-day assembly + orchestration) synthetic-tested (Step 3a). Four values frozen BLIND pre-run + CPCV/PBO structural params pinned. | Machinery: fresh-checkout green throughout (ruff/black/mypy 97 files; 227 passed/3 skip; freeze 20 vendored, adapter sole `lab.*`; 9/9 goldens; machinery-removal + literal-N sentinel-spy). Blind run: 49 NIFTY-50 survivors, 2015–2026, ~2790 days, N=1000, seed 20260711, single execution no rerun. | `src/xsranker/{gate,backtest}/`; `docs/RESEARCH_FINDINGS.md`; `docs/deep_dives/04`; tags `pre-registration-frozen`/`-v2` | **GATE 3 green — across-the-board KILL** (net-negative: DSR 0, CPCV −11…−29, pos-frac 0; all 6 arms × both bounds). Basis = DSR/CPCV net-negativity (not beat-random). **Two blind mid-run findings, both STOP-and-flagged to operator:** (1) the Step-2 ₹5M gross floor was ill-posed (cost is scale-invariant → no cost floor) and dropped 48–71% of days → RE-FROZEN blind at ₹100k (`pre-registration-v2` @ `7e6a829`; `31cc292` kept as superseded line); (2) the random null is infeasible ~50% of draws under the ⌈k/2⌉ sector cap → beat-random half zero-padded (Phase-2 blocker, quarantined, did NOT affect the KILL). Capacity finding recorded first-class (median feasible book ₹3–5M, thin tail). Gap family retired → candidate search. |
| 2026-07-11 | Phase 1 | Data & Universe layer: `BrokerAdapter`/`Repository` over external Parquet cache (config path, read-only, no ingestion); hygiene (NSE calendar+IST session, corp-action raw+adjusted, bad-tick, gap, trailing liquidity floor); survivor-cache universe stub; full dual eligibility masks; static sector map; conservative circuit filter | Reconciliation cleared (49/5-min/2015–2026/split-bonus-adjusted; div residual accepted). GATE ZERO green (6 checks incl known-split no-spurious-gap: max adj gap 0.029 « split-scale; adjusted==raw identity; masks ~0% fire). Leakage suites green in CI (prefix-invariance + train/serve-skew, synthetic). Full gate **143 passed** (3 cache-dependent skip in CI); ruff/black/mypy-strict clean; freeze tripwire + boundary intact; predecessor `src/lab` untouched | `src/xsranker/{data,features}/`; `config/{default,sectors,integrity_reference}.yaml` + `universe/survivor_cache.yaml` | **GATE 1 green.** Liquidity floor committed (₹5cr/20d). Stamps applied (survivorship upper bound; split/bonus-adj + div deferred; masks unexercised ~0%; no ingestion). No signal/execution/null/gate code. **Bug caught + fixed:** `daily_open_close` mis-computed the last-bar-per-day index (a reverse-`np.unique` trick), which corrupts `overnight_gap` and thus both gate-zero AND the Phase-2 gap signal's foundation; caught by `test_split_removes_the_spurious_gap` (the corp-action teeth test, 0.14 → 0.0289); post-fix split-gap **0.0289 stable across 3 independent re-runs** — live proof the integrity suite can go red. Cache path is config (env-var `XSR_DATA_CACHE_PATH` override + relative placeholder; no absolute machine path committed). **Two issues CI caught that local missed** (local ruff/black/mypy honor `.gitignore` and ran against the installed editable package, masking both): (a) an unanchored `.gitignore` `data/` pattern silently excluded the entire `src/xsranker/data/` source from commits — fixed by anchoring to `/data/` and committing the 19 files; (b) environment-dependent isort classification (I001) — fixed with `known-first-party` + `detect-same-package=false`, verified on a `git archive` + fresh `uv sync` extract (the true CI-equivalent). Final green commit `cf39d04`. |
| 2026-07-12 | Candidate #2 build (steps 1–2) | **Candidate #2 (volume-delta / abnormal directional participation) pre-registration FROZEN** (`3cb7b15`, tag `candidate-2-preregistration`) + `docs/END_GOAL_AND_ROADMAP.md` recorded; **`gate-3` tag cut → `2ccbb64`** (the corrected final KILL; supersedes `ddbe621`'s false basis). **Step 1 (R2 ledger fix):** durable committed `ledger/` + a **fail-closed** integrity guard (`src/xsranker/ledger/`; the gate raises before any DSR if a prior candidate's streams are missing) [1A `dd79b3f`]; regenerated candidate #1's **SIX** streams via the new committed `scripts/regenerate_ledger.py` (frozen pipeline, seed 20260711, original **Corwin-Schultz** corridor; six streams not re-pricings per operator ruling; cost-realism re-runs recorded in provenance, not rows) [1B `1bb7f64`]. **Step 2 (cost swap):** live corridor CS → **verified fees + fixed 5/18 bps, impact-coeff 0, Corwin-Schultz retired** (`config` mode `fixed_spread`; `cost.py` mode-dispatch; the regen pins CS explicitly so the historical ledger ≠ the live corridor — coexistence with no conflation); per-arm **null-health** INFO aggregate added to `run_arm`, routine `day_dropped` demoted to DEBUG. | **REPRODUCTION VERIFIED** — the regen reproduces RESEARCH_FINDINGS §8 EXACTLY (all six surviving-day counts / signal-drop %s / null-draw-drop 0% / null median ≈ −234 bps pess / short-ban 0) and **effective-N = 5.9816 @ N=1000** (5.9831 @ N=50). Fresh-checkout green at 1A + 1B (the six streams are committed, not gitignored — the R2 failure class; the self-consistency test passes on a clean extract). Cost-swap: the fixed corridor is proven **independent of the CS spread AND participation** (`test_fixed_cost_is_independent_of_cs_spread_and_participation`) — candidate #2 cannot inherit the CS over-read. | `src/xsranker/ledger/`, `scripts/regenerate_ledger.py`, `ledger/` (6 streams + `MANIFEST.yaml`), `src/xsranker/execution/cost.py` + `config/default.yaml`; tags `candidate-2-preregistration`, `gate-3` | **FINDING (first-class): the R2 defect cost us the streams, but the numbers survive verification.** Candidate #1's corrected Phase-3 KILL is now **verified reproducible from committed code** — it was NOT before (R2's ephemeral streams were lost; only a number in a doc survived). This **retroactively validates the corrected Phase-3 close**, and closes the Rule-4 persistence hole: the fail-closed guard means no future candidate's effective-N can ever be silently undercounted. **HELD after 1B per operator; step 2 pushed pending fresh-checkout.** D8 (momentum-proxy independence, blind) remains the hard pre-verdict checkpoint. |
| 2026-07-12 | Candidate #2 — D8 STOP → V_resid KILL | **D8 STOP:** raw V / abnormal V-A rank the cross-section **~0.72–0.75 (xs-Spearman)** like the morning open→entry return — the pre-registered 0.5–0.8 STOP band; the bar close-vs-open sign that signs V *is* a price measure. **V/V-A RETIRED pre-verdict** (independent of candidate #1's gap, |ρ| ≤ 0.13). Operator ruled **option (b):** build **V_resid** = per-day cross-sectional OLS residual of raw V on the morning return (rank on residual; CONTINUATION; single signal, 3 window-arms), with its **own blind pre-registration** (frozen `1cd6777`, tag `vresid-preregistration`). Verdict-run machinery: `build_book(continuation=)`, `build_symbol_days(signal_override=)`, `scripts/run_vresid_verdict.py`; panel pickle (`universe_panel.py`, 0.35 s vs 14-min reload). | **D8-on-V_resid PASSED** — vs morning return Pearson **−0.000** (linearly orthogonal by construction, verified) / Spearman ~0.11–0.13 (independent, a small nonlinear remnant — honest scoping recorded); vs gap |ρ| ≤ 0.03. **V_resid VERDICT = KILL, all 3 arms** — beat-random **46–63** (coin-flip), DSR **0**, CPCV med ~0, abs-net **−19 / −45 bps** both bounds (gross ≈ 0 → a true null, not a cost-eaten edge). Cumulative **effective-N = 8.97** (9 raw arms — D6 clustering held). Fresh-checkout green throughout. | `docs/PREREGISTRATION_VOLUME_DELTA_RESIDUAL.md`; `scripts/{d8_independence,run_vresid_verdict}.py`; `ledger/` (9 streams: cand-#1 6 + `candidate-2r-vresid` 3); tag `vresid-preregistration` | **Candidate #2 (volume-delta axis) EXHAUSTED.** The promising half (V/V-A) was **recycled dead momentum** (D8-retired, return-blind → NOT charged); the genuinely independent half (V_resid, price-orthogonal) is **empty** (KILL, run-on-returns → CHARGED). **Cleaner KILL than candidate #1** (real +12.6 bps gross edge cost destroyed vs V_resid's *no edge at all*). **Ledger-charge bright line established** (return-blind screen → not charged; return-evaluated run → charged always). D8 independence screen is now a reusable pre-verdict step. Routes to **candidate #3 (sector-relative, intraday-reversal)**. |
| 2026-07-12 | Candidate #3 — screen CLEAR + cost corridor RE-PIN | **Machinery `772dfef` (pushed):** SR = morning open→entry return − leave-one-out same-sector-peer mean; SR-Z = SR÷ATR%; reusable `independence_screen.py` (extracted from D8); leave-one-out/mean-zero/own-value-excluded/per-day-independence teeth. **Independence screen BLIND — DISTINCTNESS GATE CLEARED:** SR & SR-Z vs banked cand-#1 gap **\|xs-Spearman\| 0.16–0.19 ≪ 0.5** (all 6 rows) → distinct; D1 diagnostic 37/49 rankable (12 excluded, exact). corr(SR, morning return) ~**0.77** recorded VERBATIM (SR is largely intraday reversal in a sector wrapper; sector-demean = ~41% of rank variance → retention-gate question). **COST CORRIDOR RE-PINNED BLIND to deployment economics** (₹1L, k=5 → ₹10k/name): size-aware fees @ ₹10k = **10.605 bps** (was ~4.5 @ liquidity-max book notional — the understatement) + NSE-published-impact spread (**Abdi-Ranaldo RETIRED** like CS): opt **1** / pess **5** bps → break-even **11.6 / 15.6 bps/day**. | Return-blind liquidity check (2814d): SR-selected names at liquidity pctile **0.55** (slightly deeper than neutral), ₹10k participation median **0.01%** of morning volume — SR does NOT select the thin tail (structural difference from cand-#1's extreme-gap picks). NSE **0.02% @ ₹50L** corroborated (impact = degradation vs bid-offer mid, walks book). Fees FLAT 10.605 bps below ₹66.7k/name → **k is NOT a fee lever at ₹1L** (operator correction, recorded). Full suite **262 passed**/3 skip, mypy 71, ruff/black clean; 3 new harness teeth (fees @ deployment notional, book-independent; strictly dearer than book; CS regen unchanged). | `sector_relative.py`, `independence_screen.py`, `candidate3_screen.py`, `config/default.yaml` + `execution/config.py` + `backtest/harness.py` + tests; `RESEARCH_FINDINGS §7.5`; cand-#3 pre-reg POST-FREEZE ADDENDUM; tag `cost-corridor-repin` | **Distinctness gate cleared → single verdict run AUTHORIZED under the re-pinned corridor** (a strictly-later commit). First candidate BOTH independent AND with a valid intraday-accrual mechanism. **Small-capital penalty recorded first-class (§7.5):** break-even 11.6–15.6 @ ₹1L vs ~6.5 @ ₹50L — same signal viable at scale, dead at ₹1L; the program's most important economic constraint (a KILL at ₹1L ≠ dead at scale). **Re-pin does NOT resurrect cand-#1** (12.6 gross clears opt 11.6 but fails pess 15.6; survivorship-inflated → stays dead — not a rescue). Verdict stamps: exploration-grade / survivorship-asterisked / **₹10k integer-share tracking error (material, uncosted)** / capital-dependent bar. Floor-v2 "cost scale-invariant" premise corrected (fees size-dependent; ₹100k value stands on integer-share basis). |
| 2026-07-12 | Candidate #3 — VERDICT: KILL (independent but empty) | **Single blind verdict run** under the RE-PINNED corridor: SR + SR-Z REVERSAL × {15,30,45} = 6 arms. **KILL all 6** (5 kill + 1 near-threshold; none pass). Gross **positive but ~1 bps** (0.44–2.41 median) at **50–52% days-positive (a coin flip)**; **DSR ≈ 0** (indistinguishable from noise after deflation); median net **−20 to −31 bps** both bounds → fails the absolute-net AND DSR gates. beat-random 71–99 is a MEAN rank on a right-skewed stream (median net decisively negative). | Cumulative **effective-N = 13.48** (15 raw arms: cand#1 6 + cand#2r 3 + cand#3 6); PBO 0.382. Gross computed cost-free (median 0.44–2.41 bps, mean 0.72–3.02, ~51% days>0). Fail-closed ledger re-verified (15/15 streams present + non-empty); 9 persistence tests pass. | `scripts/run_candidate3_verdict.py`; `ledger/` (15 streams: +`cand3__{SR,SR-Z}__{15,30,45}`) + `MANIFEST.yaml` (15 required); RESEARCH_FINDINGS "Candidate #3" block; pre-reg addendum §E | **KILL — NOT banked (operator ruling).** Noise with a sign, not a small edge: the Entry gate requires an edge **statistically distinguishable from noise after deflation**, not merely positive-signed. **NEW STANDING RULE: independence without edge is not bankable** (SR proved independent AND empty → the feature-zoo error avoided; fusing noise stacks noise + raises the bar for zero benefit). Four first-class findings: (1) the re-pin did NOT cause the KILL — SR fails even the optimistic 11.6 bps bar by ~10 bps (estimator-free clean); (2) beat-random on a skewed stream misleads — the median net is the honest absolute statistic (sharpens the absolute-net gate); (3) the 0.77 diagnostic resolves — sector-demeaning bought independence, not edge; (4) independence ⊬ bankable. **Stable = ONE provisional member (cand#1 gap).** Three candidates tested, one banked; the fusion story still needs a second, edge-bearing, independent member. **HOLD for a strategic stock-take before candidate #4.** |
